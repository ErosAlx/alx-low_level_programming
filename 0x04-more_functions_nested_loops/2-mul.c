#include "main.h"

/**
 * mul - multiplies two integers.
 *
 * @a: takes first input.
 * @b: takes second input.
 *
 * Return: return mul.
 */

int mul(int a, int b)
{
	int mul = a * b;

	return (mul);
}
